#[ Atom Creator Mini-Game:
This application provides a togglable minigame where you can create your atom \
then try to match it to an existing element from the periodic table.
]#

import atom_creator

proc runMain*() =
  echo("I am the minigame!")
